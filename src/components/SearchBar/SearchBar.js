import "./SearchBar.scss";

export default function SearchBar() {
  return (
    <input
      class="search-bar"
      type="search"
      name="searchBar"
      placeholder="Search"
    ></input>
  );
}
