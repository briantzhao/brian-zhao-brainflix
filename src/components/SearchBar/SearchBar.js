import "./SearchBar.scss";

export default function SearchBar() {
  return (
    <input
      className="search-bar"
      type="search"
      name="searchBar"
      placeholder="Search"
    ></input>
  );
}
