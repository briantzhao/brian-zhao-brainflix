import "./SearchBar.scss";

export default function SearchBar() {
  return (
    <input
      class="search-bar"
      type="text"
      name="searchBar"
      placeholder="Search"
    ></input>
  );
}
